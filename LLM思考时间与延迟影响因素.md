# LLM 思考时间与延迟影响因素

## 核心结论

需要区分两个概念：

- **内部推理时间**：模型为了理解问题、规划路径和得出结论所花的时间。
- **端到端总延迟**：从发出请求到收到完整结果的全部时间。

可以粗略表示为：

```text
端到端总延迟
≈ 网络与排队时间
+ 输入预填充时间（Prefill）
+ 内部推理时间（Reasoning）
+ 输出生成时间（Decode）
+ Tool 执行与工作流调度时间
```

## 内部推理时间的主要影响因素

### 1. 任务难度

任务要求的认知操作越复杂，通常需要越多推理。例如：

```text
字段提取
< 固定分类
< 按规则选择 Tool
< 多 Tool 规划
< 复杂代码诊断或数学证明
```

### 2. 决策空间

决策空间是模型在每一步拥有的合理选择数量。

- 只有“成功或失败”两个固定分支：决策空间较小。
- 面对大量 Tool 并自行决定调用顺序：决策空间较大。

Skill 可以提前规定流程和分支，缩小模型的决策空间。

### 3. 推理深度

推理深度指完成任务需要连续推导多少步。

```text
读取日志
→ 定位异常请求
→ 追踪调用链
→ 对照代码
→ 验证假设
→ 判断根因
```

即使每一步的选择不多，依赖链很长也会增加推理时间。

### 4. 输入的信息复杂度

需要同时考虑输入长度与理解难度：

- **输入长度**主要影响预填充时间。
- **信息关联、冲突和干扰程度**会进一步影响内部推理时间。

同样是 10,000 Token，只读取最后一段和交叉比较全文，推理难度并不相同。

### 5. 不确定性与歧义

目标和约束越模糊，模型越需要建立假设并权衡方案。

```text
“优化一下系统”
```

比下面的要求具有更大的搜索空间：

```text
“在不改变接口的前提下，将查询 P95 延迟降低到 200 ms。”
```

### 6. 异常、纠错与重新规划

Tool 调用失败、结果不完整或结果与预期矛盾时，模型需要分析原因并修改计划，因此会增加推理时间。

### 7. 模型与推理配置

还会受到以下因素影响：

- 模型规模与架构；
- reasoning effort；
- 推理 Token 上限；
- 是否复用历史状态；
- 输出格式约束。

更高的推理强度通常允许模型投入更多计算，也可能带来更高延迟。

## 输入处理需要拆成两部分

### 输入预填充

模型需要读取系统提示词、对话历史、Skill、Tool 定义、用户问题和 Tool 返回结果。输入越长，预填充通常越慢。

### 输入理解

模型还需要判断信息相关性、规则优先级、流程状态及内容冲突。这部分会影响内部推理。

一句话概括：

> 输入有多长决定“读多久”，问题有多难、路径有多宽和多深决定“想多久”，输出有多长决定“写多久”。

## ReAct 与 Skill 的延迟对比

在 LLM 调用次数和 Tool 调用次数相同的情况下：

| 范式 | 输入特点 | 决策特点 |
|---|---|---|
| ReAct | 提示可能较短 | 每轮可能需要开放式规划，决策空间较大 |
| Skill | 增加 Skill 正文 | 流程和分支更明确，决策空间较小 |

因此，Skill 是否更快取决于：

```text
缩小决策空间节省的推理时间
是否大于
读取 Skill 正文增加的输入处理时间
```

Skill 并不必然更快，但通常能提高执行的稳定性和可预测性。若 Skill 通过确定性程序直接编排多个 Tool，还可以减少中间的 LLM 调用轮次，从而获得更明显的速度优势。

## 面试回答

> LLM 的内部推理时间主要受到任务难度、决策空间、推理深度、信息复杂度、不确定性和模型推理配置的影响。输入长度主要影响预填充时间，而信息的关联和理解难度还会影响内部推理。端到端延迟则还要加上网络排队、输出生成、Tool 执行以及多轮工作流调度时间。

